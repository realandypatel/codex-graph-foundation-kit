# Closing Prompt

```text
We are closing this session.

Before stopping:
1. Update `WORKLOG.md`
2. Update `TOMORROW.md`
   - add newly discovered bugs
   - add new work items
   - update progress
   - remove only items that are truly complete
   - keep CRITICAL items at the top until fully resolved
3. Run `git status --short`
4. Separate real work from noise
5. List any background processes started during this session, but do not kill anything without asking me first
6. Give me a concise summary of:
   - what was completed
   - what is still pending
   - what should happen next session

Rules:
- do not touch production unless explicitly asked
- do not auto-fix unresolved items without approval
- do not commit memory files unless I explicitly ask
```
