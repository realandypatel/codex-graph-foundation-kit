Starting a new session in this repository.

Do the following in order:
1. Read `PROJECT_CONTEXT.md`
2. Read `TOMORROW.md`
3. Read `WORKLOG.md`
4. Check graph status and confirm the graph is current
5. Confirm graph tools are available

Working rules:
- do not scan the whole repo by default
- never read node_modules unless explicitly asked
- use graph tools first
- read only the minimum files needed
- for large files, read only needed ranges
- reuse already-read context
- summarize shell output briefly
- ask before risky or speculative changes

Then tell me:
- whether the graph is current
- what the highest-priority task is
- what you recommend doing first

Then wait for direction.
